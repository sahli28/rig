node_modules
.next
.expo
dist
build
coverage
.turbo
pnpm-lock.yaml
supabase/.temp
supabase/.branches

# Prose et configuration rédigées à la main : Prettier réaligne les tableaux
# markdown sans rien apporter, et pollue les diffs des tickets.
docs/
.claude/

# Exports du bureau Claude — même raison que le .gitignore.
Claude outputs/

# Config client Firebase (P2-024) : fichier généré par Firebase, versionné
# (public par construction, décision D-031). On le laisse tel que Firebase le
# rend — Prettier ne le réaligne pas.
apps/mobile/google-services.json
