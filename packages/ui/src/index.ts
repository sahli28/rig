/**
 * Entrée par défaut de `@rack/ui` : le thème seul, sans dépendance plateforme.
 *
 * Le kit de composants React Native vit derrière `@rack/ui/native`, pour que le
 * web puisse importer les tokens sans embarquer React Native.
 */
export * from './theme/index';
